type Apartment @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  description: String!
  street: String!
  streetNo: Int!
  postalCode: String
  city: String!
  apartmentNo: Int
  price: Float
  capacity: Int
  images: [String]
}
